import { ReactNode } from 'react';
export type LayoutProps = {
  title: string;
  subTitle?: string;
  children?: ReactNode;
};

export type MainContentProps = {
  title: string;
  subTitle?: string;
  children?: ReactNode;
};
